{
  "name": "scrape-test",
  "version": "1.0.0",
  "description": "This is a three step test. Each step should include instructions on how to run the code.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1"
  }
}
